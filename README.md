# aws-fullstack-components

Implementación y uso de los componentes más ampliamente utilizados en AWS Fullstack Development.

## Estructura del proyecto

La estructura del proyecto está organizada de la siguiente manera:

```bash
curl --location 'http://localhost:8080/lambda/invoke' \
--header 'Content-Type: application/json' \
--data '{
    "username": "raul"
}'
```
